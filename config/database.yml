default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

development:
  <<: *default
  database: <%= ENV["POSTGRES_DB"] %>

test:
  <<: *default
  url: <%= ENV["POSTGRES_DB_TEST"] %>

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
